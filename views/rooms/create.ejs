<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เพิ่มห้องพัก - ระบุตำแหน่ง</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style> body { font-family: 'Sarabun', sans-serif; } </style>
</head>
<body class="bg-slate-50 text-slate-800">

    <%- include('../partials/sidebar') %>

    <div class="ml-64 pt-16 min-h-screen flex flex-col">
        <%- include('../partials/navbar') %>

        <main class="flex-1 p-8 flex justify-center">
            <div class="w-full max-w-3xl">
                
                <div class="mb-6">
                    <a href="/rooms" class="text-slate-500 hover:text-blue-600 text-sm mb-2 inline-block">← กลับไปหน้ารายชื่อ</a>
                    <h1 class="text-2xl font-bold text-slate-800">เพิ่มห้องพัก (ระบุตำแหน่ง)</h1>
                    <p class="text-slate-500">กำหนดตึกและชั้น เพื่อสร้างผังห้องพักที่ถูกต้อง</p>
                </div>

                <div class="bg-white p-8 rounded-xl shadow-sm border border-slate-200">
                    <form action="/rooms/create" method="POST">
                        
                        <div class="mb-8 pb-6 border-b border-slate-100">
                            <h2 class="text-lg font-bold text-slate-700 mb-4 flex items-center gap-2">
                                📍 ตำแหน่งห้องพัก (Location)
                            </h2>
                            <div class="grid grid-cols-3 gap-6">
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">ตึก / อาคาร</label>
                                    <select name="building" class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-slate-50">
                                        <option value="A">อาคาร A (ด้านหน้า)</option>
                                        <option value="B">อาคาร B (ด้านหลัง)</option>
                                        <option value="C">อาคาร C (โรงจอดรถ)</option>
                                    </select>
                                </div>
                                
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">ชั้นที่</label>
                                    <select name="floor" class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-slate-50">
                                        <option value="1">ชั้น 1</option>
                                        <option value="2">ชั้น 2</option>
                                        <option value="3">ชั้น 3</option>
                                        <option value="4">ชั้น 4</option>
                                    </select>
                                </div>

                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">หมายเลขห้อง</label>
                                    <input type="text" name="room_number" placeholder="เช่น 101" 
                                           class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none font-bold text-slate-800">
                                    <p class="text-xs text-slate-400 mt-1">*ระบบจะใช้เลขนี้แสดงในผัง</p>
                                </div>
                            </div>
                        </div>

                        <div class="mb-6">
                            <h2 class="text-lg font-bold text-slate-700 mb-4 flex items-center gap-2">
                                🏠 รายละเอียดห้อง (Details)
                            </h2>
                            <div class="grid grid-cols-2 gap-6 mb-4">
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">ราคาค่าเช่า (บาท/เดือน)</label>
                                    <input type="number" name="price" value="3500" 
                                           class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none">
                                </div>
                                <div>
                                    <label class="block text-sm font-medium text-slate-700 mb-2">ประเภทห้อง</label>
                                    <select name="type" class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none bg-white">
                                        <option value="Standard">Standard (พัดลม)</option>
                                        <option value="VIP">VIP (แอร์ + เฟอร์ฯ)</option>
                                        <option value="Corner">Corner (ห้องมุม)</option>
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="flex items-center justify-end gap-3 pt-6 border-t border-slate-100">
                            <a href="/rooms" class="px-6 py-2 text-slate-600 hover:bg-slate-100 rounded-lg transition">ยกเลิก</a>
                            <button type="submit" class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-lg shadow-blue-200 transition font-medium">
                                + สร้างห้องพักตามนี้
                            </button>
                        </div>

                    </form>
                </div>
            </div>
        </main>
    </div>
</body>
</html>